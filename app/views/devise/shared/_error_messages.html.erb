<% if resource.errors.any? %>
  <div class="bg-red-100 text-red-700 rounded-lg p-4 mb-4">
    <ul>
      <% resource.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>
